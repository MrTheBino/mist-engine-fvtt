<div class="npc-beautified-container-stats flexrow">
    <div class="left-half">
        {{!-- Limits --}}
        <div class="npc-limits-container">
            <div class="npc-subtitle-container-c">
                <div class="npc-subtitle-text">{{localize "MIST_ENGINE.LABELS.Npc.limits"}}</div>
            </div>
            <div class="npc-limits-list flexcol">
                {{#each system.limits as |limit|}}
                <div class="npc-limit-item flexrow">
                    <div class="npc-limit-name">{{limit.name}}</div>
                    <span class="npc-limit-value-armor"><span class="npc-limit-value">{{npcLimitValue limit.value}}</span></span>
                </div>
                {{/each}}
            </div>
        </div>
        {{!-- Tags & Statuses --}}
        <div class="npc-tags-container">
            <div class="npc-subtitle-container-c">
                <div class="npc-subtitle-text">{{localize "MIST_ENGINE.LABELS.Npc.TagsAndStatuses"}}</div>
            </div>
            <div class="npc-tags-list flexcol">
                {{#each system.tags_and_statuses as |tagOrStatus|}}
                <div class="npc-tag-item">
                    <div class="npc-tag-name icon-hover">
                        {{#if (gt tagOrStatus.value 0)}}
                        <span class="npc-tag-status-value" data-action="addSceneAppRollMod" data-value="{{tagOrStatus.value}}" data-name="{{tagOrStatus.name}}">{{tagOrStatus.name}}-{{tagOrStatus.value}}</span>
                        {{else if (tagFilled tagOrStatus.symbol)}}
                        <i class="tag-icon-litm-{{tagOrStatus.symbol}}"></i>&nbsp;
                        <span class="npc-tag-with-symbol" data-action="addSceneAppRollMod" data-value="0" data-name="{{tagOrStatus.name}}">{{tagOrStatus.name}}</span>
                        {{else}}
                        <span class="npc-tag" data-action="addSceneAppRollMod" data-value="0" data-name="{{tagOrStatus.name}}">{{tagOrStatus.name}}</span>
                        {{/if}}
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
        {{!-- Special Features --}}
        <div class="npc-limits-container">
            <div class="npc-subtitle-container-c">
                <div class="npc-subtitle-text">{{localize "MIST_ENGINE.LABELS.Npc.SpecialFeatures"}}</div>
            </div>
            <div class="npc-limits-list flexcol">
                {{#each system.specialFeatures as |feature|}}
                <div class="npc-limit-item">
                    <div class="npc-limit-name">{{feature.name}}</div>
                    <div class="npc-limit-description">{{{textWithTags feature.description}}}</div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>

    <div class="right-half">
        {{!-- Threats & Consequences --}}
        <div class="npc-threats-and-consequences-container">
            <div class="npc-subtitle-container-c">
                <div class="npc-subtitle-text">{{localize "MIST_ENGINE.LABELS.Npc.ThreatsAndConsequences"}}</div>
            </div>
            {{#each system.threatsAndConsequences as |threat|}}
            <div class="entry">
                <div class="entry-header">
                    <span class="entry-name">{{threat.name}}</span> {{threat.description}}
                </div>
                <ul class="entry-list">
                    {{#each threat.list as |entry|}}
                    <li class="entry-item">{{{textWithTags entry}}}</li>
                    {{/each}}
                </ul>
            </div>
            {{/each}}
        </div>
    </div>
</div>